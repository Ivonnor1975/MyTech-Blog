<article class="post">
  <div class="title">
    <span>{{title}} {{format_date created_at}}</span>
  </div>
  <div class="meta">
    <span>{{body}} by {{username}} </span>
    <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
  </div>
</article>